# PantherLang First Real Network Intelligence Example

This example exercises PantherLang system/network capabilities against the
local host. It performs passive local discovery and explicit TCP connection
checks only against `127.0.0.1`.

Run:

    panther check examples/first_real_panther_network_intelligence/main.pan
    panther run examples/first_real_panther_network_intelligence/main.pan
