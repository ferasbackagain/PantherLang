# SQLite App

Database CRUD operations with table creation, inserts, queries, updates.

## Run

```bash
panther run projects/sqlite_app/main.pan
```

## Expected Output

Creates in-memory SQLite database, CRUD operations, then cleanup.
