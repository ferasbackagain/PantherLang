# Web Runtime Fix 1 Parser Brace Fix

Apply this after `web_runtime_fix1_real_serve` and `web_runtime_fix1_ast_export_fix` if `return { ... };` inside `web/api route` fails with `Expected ';' after return statement`.

Run:

```bash
unzip -o pantherlang_web_runtime_fix1_parser_brace_fix.zip
chmod +x bootstrap_web_runtime_fix1_parser_brace_fix.sh
./bootstrap_web_runtime_fix1_parser_brace_fix.sh
```
